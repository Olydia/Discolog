#!/bin/bash
#
# Usage: dual [ <filename> ]
#
# Runs the given script file with two agents and console and
# compares resulting temporary log with
# corresponding test log file <filename>.test, if it exists
#
dir="`dirname $0`"
"$dir/../lib/disco/bin/_run" fr.limsi.negotiate.ExampleAgent\$Dual "$1" ../../class lib/disco.jar 
